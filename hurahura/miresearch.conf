# DO NOT USE QUOTES FOR STRING
# BUT 
# FOR STRINGS IN LISTS ==> ALWAYS USE DOUBLE QUOTES - these will be json decoded

[DEFAULT]
debug=False

# Choose: HARD, SOFT, NONE
anon_level = NONE

class_path = 
stable_directory_age_sec=60
default_pad_zeros=6

[app]

data_root_dir=~
subject_prefix=MI 

directories = [["RAW", "DICOM"], "META"]
# Override DEFAULT parameters here


[parameters]
# Add application specific parameters here



# Optional DataBaser setting
#    - local SQLite index (filesystem META remains source of truth).
# [database]
# enabled = false
# db_path =
# sync_on_meta_write = true
# study_columns = StudyDate, StudyDescription, StudyID, StudyInstanceUID, PatientID, PatientSex, Modality, MagneticFieldStrength, ANONYMISED, Age
# meta_suffixes =
# meta_sync_all = true
# extra_fields = ANONYMISED

